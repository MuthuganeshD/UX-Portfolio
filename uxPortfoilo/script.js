// // JavaScript code for changing the background color dynamically
// function changeBackgroundColor() {
//     const colors = ['#3498db', '#e74c3c', '#2980b9', '#c0392b', '#16a085', '#d35400', '#2ecc71', '#f39c12']; // List of colors
//     const randomColor = colors[Math.floor(Math.random() * colors.length)]; // Get a random color from the list
//     document.body.style.backgroundColor = randomColor; // Apply the random color to the body background
// }

// // Call the function when the page is loaded
// window.onload = changeBackgroundColor;
